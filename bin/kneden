#!/usr/bin/env node

var kneden = require('../');

var str = '';
process.stdin.setEncoding('utf8');

process.stdin.on('readable', function () {
  var chunk = process.stdin.read();
  if (chunk) {
    str += chunk;
  }
});

process.stdin.on('end', function () {
  process.stdout.write(kneden(str));
})
